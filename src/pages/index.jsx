import Layout from "../components/Layout/Layout";

const Index = (props) => {
    return (
        <Layout>
            <div>Contenido principal</div>
        </Layout>
    );
};
export default Index;
